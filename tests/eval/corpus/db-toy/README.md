# db-toy

Prisma-based database access layer used as a codebase-oracle eval fixture.

- `src/schema.prisma` - Prisma schema file defining the User and Post models
- `src/queries.ts` - typed query helper functions wrapping the Prisma client
